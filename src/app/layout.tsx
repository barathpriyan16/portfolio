import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Barathpriyan R | AI & ML Engineer Portfolio",
  description: "Personal portfolio website of Barathpriyan R, showcasing machine learning systems, full-stack development projects, and hackathon achievements.",
  keywords: ["Barathpriyan R", "AI & ML Student", "Full Stack Developer", "Next.js Portfolio", "Sri Eshwar College of Engineering"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
